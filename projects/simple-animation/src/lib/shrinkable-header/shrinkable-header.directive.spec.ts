import { ShrinkableHeaderDirective } from './shrinkable-header.directive';

describe('ShrinkableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ShrinkableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
